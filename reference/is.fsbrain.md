# Check whether object is an fsbrain (S3)

Check whether object is an fsbrain (S3)

## Usage

``` r
is.fsbrain(x)
```

## Arguments

- x:

  any `R` object

## Value

TRUE if its argument is an fsbrain (that is, has "fsbrain" amongst its
classes) and FALSE otherwise.
