# Check whether object is an fs.coloredmesh (S3)

Check whether object is an fs.coloredmesh (S3)

## Usage

``` r
is.fs.coloredmesh(x)
```

## Arguments

- x:

  any `R` object

## Value

TRUE if its argument is a coloredmesh (that is, has "fs.coloredmesh"
amongst its classes) and FALSE otherwise.
