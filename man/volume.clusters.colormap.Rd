% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_volume_clusters.R
\name{volume.clusters.colormap}
\alias{volume.clusters.colormap}
\title{Compute the shared colormap of the cluster shells.}
\usage{
volume.clusters.colormap(makecmap_options, levels, max_abs = max(abs(levels)))
}
\arguments{
\item{makecmap_options}{named list, the colormap options, see \code{\link[squash]{makecmap}}.}

\item{levels}{numerical vector of iso-levels.}

\item{max_abs}{single positive number, the absolute value of the most extreme value that the colormap has to cover, i.e., half of the range of the colormap. Defaults to the highest absolute iso-level.}
}
\value{
named list with the entries 'map' (the \code{squash} colormap), 'colors' (a color per entry of \code{levels}), 'range' (the range of the colormap) and 'options' (\code{makecmap_options}, including the range, for the colorbar metadata of the meshes).
}
\description{
All shells of both signs share one colormap, so that the color of a shell is exactly the color that the colorbar shows for its iso-level. A symmetric range which covers all iso-levels is used unless the user requested a range via the 'range' entry of \code{makecmap_options}.
}
\keyword{internal}
