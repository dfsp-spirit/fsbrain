% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_volume_clusters.R
\name{volume.cluster.levels}
\alias{volume.cluster.levels}
\title{Compute the iso-levels of the cluster shells.}
\usage{
volume.cluster.levels(
  volume,
  threshold,
  num_levels = 10L,
  max_level = NULL,
  max_level_quantile = 0.99,
  positive = TRUE,
  negative = TRUE
)
}
\arguments{
\item{volume}{a 3D numerical array.}

\item{threshold}{single positive number, the cluster threshold.}

\item{num_levels}{positive integer, the number of levels per sign.}

\item{max_level}{single positive number or \code{NULL}, the highest level. If \code{NULL}, it is computed per sign as the \code{max_level_quantile} quantile of the absolute values beyond the threshold.}

\item{max_level_quantile}{single value between 0 and 1, the quantile used to compute \code{max_level} if that is \code{NULL}.}

\item{positive}{logical, whether to compute levels for the positive side.}

\item{negative}{logical, whether to compute levels for the negative side.}
}
\value{
numerical vector of iso-levels, the negative ones (if any) first, each ordered from the outermost to the innermost shell.
}
\description{
Computes the iso-levels at which the shells of the clusters are extracted: \code{num_levels} levels from the threshold up to the highest level, for each requested sign, ordered from the outermost (the threshold) to the innermost (the highest level) shell.
}
\keyword{internal}
