% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_volume.R
\name{compute.surface.contour.slices}
\alias{compute.surface.contour.slices}
\title{Compute surface contour slice images (internal).}
\usage{
compute.surface.contour.slices(
  subjects_dir,
  subject_id,
  volume = "brain",
  surface = "white",
  hemi = "both",
  surface_color = "#FF0000",
  surface_lwd = 1,
  slices = -5,
  axis = 1L,
  silent = TRUE
)
}
\arguments{
\item{subjects_dir}{character string, the FreeSurfer SUBJECTS_DIR.}

\item{subject_id}{character string, the subject identifier.}

\item{volume}{numeric 3D array or character string. Either a 3D brain volume or the name of a volume file to load from the subject's \code{mri/} directory. Defaults to \code{"brain"}.}

\item{surface}{character string or vector of strings, the surface(s) to use for contour extraction.}

\item{hemi}{character string, one of \code{'lh'}, \code{'rh'}, or \code{'both'}.}

\item{surface_color}{character string or vector, color(s) for the contour lines.}

\item{surface_lwd}{numeric, line width for contours.}

\item{slices}{passed to \code{\link[fsbrain]{volvis.lightbox}}. A negative integer N means "use every Nth slice". A numeric vector gives explicit slice indices (1-based). Defaults to \code{-5}.}

\item{axis}{integer, the slice axis. 1 = sagittal, 2 = coronal, 3 = axial. Defaults to \code{1L}.}

\item{silent}{logical, whether to suppress messages.}
}
\value{
named list with entries: \code{images} (list of magick images, one per slice), \code{slice_indices} (integer vector of 1-based slice indices used), \code{has_contour} (logical vector, TRUE if at least one contour segment was drawn on the slice). Also includes metadata entries \code{axis}, \code{surfaces}, \code{hemis}, \code{subject_id} for downstream use.
}
\description{
Internal helper that loads a volume and surface meshes, computes the intersection contours of each surface with each slice plane, and draws them onto individual magick slice images. Used by both \code{\link{volvis.lb.with.surface}} (which grids the results) and \code{\link{volvis.slices.with.surface}} (which exports them individually).
}
\keyword{internal}
