% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol2surf.R
\name{vol.tkreg.affine}
\alias{vol.tkreg.affine}
\title{Transform a volume affine matrix into the FreeSurfer tkregister convention.}
\usage{
vol.tkreg.affine(affine, dims)
}
\arguments{
\item{affine}{numeric 4x4 matrix, the transformation matrix from the volume header (maps 0-based voxel indices to world coordinates).}

\item{dims}{integer vector of length 3 or more, the dimensions of the volume (\code{dim(volume_data)}).}
}
\value{
numeric 4x4 matrix, the tkregister transformation matrix.
}
\description{
FreeSurfer stores brain surfaces (and all surface-based data) in the \emph{surface RAS} coordinate system, which is also called the \emph{tkregister} or \emph{tkreg} space: the RAS coordinate (0, 0, 0) is at the center of the field of view of the volume. The transformation from voxel indices to this space is \strong{not} the transformation stored in the header of the volume: a volume can have a center-of-RAS offset (\code{cras}), which is preserved by FreeSurfer in the header but ignored by the surface-based tools (this is the difference between the \code{--vox2ras} and \code{--vox2ras-tkr} options of \code{mri_vol2surf}).

This function takes the header transformation matrix of a volume and returns the corresponding tkregister matrix, i.e., the same direction cosines and voxel sizes, but with the translation replaced so that the center of the volume is at RAS (0, 0, 0). For a conformed FreeSurfer volume (256x256x256 voxels of 1 mm), the result is identical to \code{\link[fsbrain]{vox2ras_tkr}}.
}
\keyword{internal}
