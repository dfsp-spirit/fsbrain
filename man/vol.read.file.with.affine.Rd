% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol2surf.R
\name{vol.read.file.with.affine}
\alias{vol.read.file.with.affine}
\title{Load a volume file and return the data along with the affine transformation.}
\usage{
vol.read.file.with.affine(filepath)
}
\arguments{
\item{filepath}{character string, the path to an \code{.mgz}, \code{.mgh}, \code{.nii} or \code{.nii.gz} file.}
}
\value{
named list with entries: 'data' (numeric array, 3D or 4D), 'affine' (4x4 numeric matrix), 'filepath' (character string), 'file_format' (character string, either 'mgh' or 'nifti'), and 'frames' (integer, the number of volumes in a 4D file).
}
\description{
Supports MGH/MGZ volumes (read with \code{\link[freesurferformats]{read.fs.mgh}}) and NIfTI v1 volumes, including oblique ones (read with \code{\link[freesurferformats]{read.fs.volume.nii}}, which requires the 'oro.nifti' package). The data array is returned in the storage order of the file, i.e., it is \emph{not} reoriented, and the returned affine maps \strong{0-based} voxel indices (column, row, slice) to world coordinates. This is the convention used by the NIfTI \code{sform} and by \code{\link[freesurferformats]{mghheader.vox2ras}}.
}
\note{
If the header of an MGH/MGZ file does not contain a valid \code{vox2ras} matrix (\code{ras_good_flag} is 0), the surface RAS convention \code{\link[fsbrain]{vox2ras_tkr}} is used for conformed volumes (dimension 256x256x256) and a warning is emitted. For NIfTI files without a valid sform/qform, an error is raised, as the coordinate space is unknown in that case.
}
\keyword{internal}
