% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sphere_cloud.R
\name{coloredmesh.from.spheres}
\alias{coloredmesh.from.spheres}
\title{Create a coloredmesh that draws a set of spheres (e.g., connectome nodes).}
\usage{
coloredmesh.from.spheres(
  centers,
  radii = 1,
  col = "#FF0000",
  subdivisions = 2L,
  metadata = list(),
  hemi = NULL,
  style = NULL
)
}
\arguments{
\item{centers}{n x 3 numeric matrix, the sphere centers.}

\item{radii}{numeric vector of length n or a single number, the sphere radii.}

\item{col}{vector of hex color strings, a single one or one per sphere.}

\item{subdivisions}{non-negative integer, the number of subdivisions of the unit icosphere. Defaults to 2L.}

\item{metadata}{named list, metadata for the resulting coloredmesh. See \code{\link[fsbrain]{fs.coloredpaths}} for the metadata convention which enables colorbars.}

\item{hemi}{character string or NULL, the hemisphere this renderable belongs to. Defaults to NULL (not hemisphere-specific).}

\item{style}{\code{NULL} or a rendering style for this mesh, see \code{\link[fsbrain]{get.rglstyle}}.}
}
\value{
fs.coloredmesh instance.
}
\description{
Builds one mesh containing all spheres, with a per-vertex color taken from the color of the sphere the vertex belongs to. The result is a regular \code{\link[fsbrain]{fs.coloredmesh}} instance, so it can be rendered, transformed and exported like any other mesh. Note that the vertices of a sphere are not shared with any other sphere, so each sphere can have its own radius and color.
}
\examples{
  cm = fsbrain:::coloredmesh.from.spheres(rbind(c(0, 0, 0), c(10, 0, 0)), c(1, 2), c("#FF0000", "#00FF00"));
  class(cm);

}
\keyword{internal}
