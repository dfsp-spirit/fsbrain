% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sphere_cloud.R
\name{spheres.mesh}
\alias{spheres.mesh}
\title{Compute the mesh of a set of spheres.}
\usage{
spheres.mesh(centers, radii = 1, subdivisions = 2L)
}
\arguments{
\item{centers}{n x 3 numeric matrix, the sphere centers.}

\item{radii}{numeric vector of length n or a single number, the sphere radii.}

\item{subdivisions}{non-negative integer, the number of subdivisions of the unit icosphere, see \code{\link[fsbrain]{unit.icosphere}}. Defaults to 2L.}
}
\value{
an fs.surface instance (a mesh) with one component per sphere.
}
\description{
Merges one slightly refined icosphere per sphere into a single mesh, i.e., the result is a single surface with one connected component per input sphere. This is much cheaper to render (and to convert for the scimesh backend) than a list of hundreds of separate sphere meshes.
}
\examples{
  m = fsbrain:::spheres.mesh(rbind(c(0, 0, 0), c(10, 0, 0)), c(1, 2));
  nrow(m$vertices);

}
\keyword{internal}
