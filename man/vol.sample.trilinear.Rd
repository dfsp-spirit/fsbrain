% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol2surf.R
\name{vol.sample.trilinear}
\alias{vol.sample.trilinear}
\title{Sample a 3D volume at the given voxel coordinates using trilinear interpolation.}
\usage{
vol.sample.trilinear(volume_data, coords)
}
\arguments{
\item{volume_data}{numeric 3D array, the volume data.}

\item{coords}{numeric matrix with 3 columns and one row per query point, the \strong{0-based} voxel coordinates (column, row, slice). Coordinates must be within the volume (see \code{\link[fsbrain]{vol.clamp.coords}}), or an error will be raised.}
}
\value{
numeric vector, one interpolated value per query point.
}
\description{
Sample a 3D volume at the given voxel coordinates using trilinear interpolation.
}
\note{
Coordinates which are exactly on the last voxel result in the value of that voxel: the interpolation weights of the out-of-range upper neighbors become 0, and their index is clamped (which is safe because their weight is 0).
}
\keyword{internal}
