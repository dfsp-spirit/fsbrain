% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optdata.R
\name{download_fs_LR_32_labels}
\alias{download_fs_LR_32_labels}
\title{Download label files for the fs_LR 32k template.}
\usage{
download_fs_LR_32_labels(scheme = "https")
}
\arguments{
\item{scheme}{character string, the URL scheme to use. Either \code{"https"} (the default) or \code{"http"}. Switching to \code{"http"} can be useful as a fallback if the HTTPS server is unreachable.}
}
\value{
Named list. The list has entries: "available": vector of strings. The names of the files that are available in the local file cache. You can access them using get_optional_data_filepath(). "missing": vector of strings. The names of the files that this function was unable to retrieve.
}
\description{
Download a set of label files for the fs_LR 32k template (the HCP-style surface space), based on a declarative manifest file shipped with this package. Currently this is the cortex label (\code{?h.cortex.label}), which defines the medial wall: all vertices which are \emph{not} part of the label are medial wall vertices. It can be used to mask the medial wall when projecting volume data to the fs_LR 32k surface, see \code{\link[fsbrain]{template.vol2surf}} (parameter \code{cortex_only}). This data is not required for the package to work.
}
\examples{
\dontrun{
   fsbrain::download_fs_LR_32_labels();
   subjects_dir = fsbrain::get_optional_data_filepath("subjects_dir");
   cortex_lh = subject.label(subjects_dir, "fs_LR_32", "cortex.label", "lh");
}

}
\seealso{
Other fs_LR 32k template functions:
\code{\link[=download_fs_LR_32_atlases]{download_fs_LR_32_atlases()}},
\code{\link[=download_fs_LR_32_meshes]{download_fs_LR_32_meshes()}}
}
\concept{fs_LR 32k template functions}
