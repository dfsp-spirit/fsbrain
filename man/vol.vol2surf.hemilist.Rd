% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol2surf.R
\name{vol.vol2surf.hemilist}
\alias{vol.vol2surf.hemilist}
\title{Project a volume onto the vertices of the given surfaces (internal workhorse).}
\usage{
vol.vol2surf.hemilist(
  surfaces,
  volume,
  default_dir,
  interpolation,
  frame,
  clamp,
  check_fov,
  affine = NULL,
  vox2ras = "auto"
)
}
\arguments{
\item{surfaces}{named list of vertex coordinate matrices, per hemisphere.}

\item{volume}{the volume, as accepted by \code{\link[fsbrain]{vol.load}}.}

\item{default_dir}{character string or NULL, the directory used to resolve volume names without extension.}

\item{interpolation}{character string, see \code{\link[fsbrain]{subject.vol2surf}}.}

\item{frame}{integer scalar, see \code{\link[fsbrain]{subject.vol2surf}}.}

\item{clamp}{logical, whether to clamp query points outside the volume to the volume border.}

\item{check_fov}{logical, whether to check (and warn about) vertices outside the volume.}

\item{affine}{numeric 4x4 matrix or \code{NULL}, used instead of the affine matrix from the volume header if given.}

\item{vox2ras}{character string, see \code{\link[fsbrain]{subject.vol2surf}}.}
}
\value{
named list of numeric vectors (one per hemisphere), with attribute 'outside' (named list of logical vectors or NULLs).
}
\description{
Project a volume onto the vertices of the given surfaces (internal workhorse).
}
\keyword{internal}
