% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol2surf.R
\name{vol.find.file}
\alias{vol.find.file}
\title{Compute the filepath of a volume file.}
\usage{
vol.find.file(volume, default_dir = NULL)
}
\arguments{
\item{volume}{character string or named list. If a character string and the file exists, it is interpreted as the volume filepath. If it does not exist, it is interpreted as the volume name without extension, resolved against \code{default_dir} (and the current working directory) by trying the extensions \code{.mgz}, \code{.mgh}, \code{.nii.gz}, and \code{.nii} (in that order). If a named list, it is interpreted as an in-memory volume (see \code{\link[fsbrain]{subject.vol2surf}}) and \code{NULL} is returned.}

\item{default_dir}{character string or NULL, the directory to resolve volume names against, typically the \code{mri} directory of a subject.}
}
\value{
character string, the volume filepath, or \code{NULL} if \code{volume} is an in-memory volume.
}
\description{
Takes a volume specification (a filepath, a name without extension, or an in-memory volume) and computes the filepath of the volume file on disk.
}
\keyword{internal}
