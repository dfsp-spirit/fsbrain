% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_volume_clusters.R
\name{volume.cluster.extremes}
\alias{volume.cluster.extremes}
\title{Compute the highest iso-level per sign.}
\usage{
volume.cluster.extremes(
  volume,
  threshold,
  max_level = NULL,
  max_level_quantile = 0.99,
  positive = TRUE,
  negative = TRUE
)
}
\arguments{
\item{volume}{a 3D numerical array.}

\item{threshold}{single positive number, the cluster threshold.}

\item{max_level}{single positive number or \code{NULL}, the highest level given by the user.}

\item{max_level_quantile}{single value between 0 and 1, the quantile used to compute the highest level if \code{max_level} is \code{NULL}.}

\item{positive}{logical, whether to consider the positive clusters.}

\item{negative}{logical, whether to consider the negative clusters.}
}
\value{
named numerical vector with the entries 'neg' and 'pos' (the highest absolute iso-level of the negative and of the positive clusters), or \code{NA} for a sign that has no voxels beyond the threshold (or was not requested).
}
\description{
Computes the highest absolute iso-level for the positive and the negative clusters, i.e., the level of the innermost shell of a cluster. If \code{max_level} is \code{NULL}, the \code{max_level_quantile} quantile of the absolute values beyond the threshold is used, so that a single extreme voxel does not dominate the visualization.
}
\keyword{internal}
