% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_volume_clusters.R
\name{context.mesh.resolve.subjects.dir}
\alias{context.mesh.resolve.subjects.dir}
\title{Resolve the subjects dir for a context mesh.}
\usage{
context.mesh.resolve.subjects.dir(subject_id = "fsaverage", context = NULL)
}
\arguments{
\item{subject_id}{string, the subject identifier.}

\item{context}{the context mesh definition, as passed by the user, see \code{\link[fsbrain]{vis.volume.clusters}}. A subject id and subjects dir can be part of this definition, in which case they take precedence.}
}
\value{
string, the path to the subjects dir to use, or \code{NULL} if the context mesh does not have to be loaded from disk (e.g., because it was passed as a pre-built mesh).
}
\description{
Determine the subjects dir from which the requested context mesh can be loaded, if the user did not pass one. For the template subject 'fsaverage' the surfaces are downloaded into the package cache if they are not available, for other subjects the standard search locations are checked, see \code{\link[fsbrain]{find.subjectsdir.of}}.
}
\keyword{internal}
