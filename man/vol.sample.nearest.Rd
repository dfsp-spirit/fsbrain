% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol2surf.R
\name{vol.sample.nearest}
\alias{vol.sample.nearest}
\title{Sample a 3D volume at the given voxel coordinates using nearest neighbor interpolation.}
\usage{
vol.sample.nearest(volume_data, coords)
}
\arguments{
\item{volume_data}{numeric 3D array, the volume data.}

\item{coords}{numeric matrix with 3 columns and one row per query point, the \strong{0-based} voxel coordinates. Coordinates must be within the volume.}
}
\value{
numeric vector, one value per query point.
}
\description{
Sample a 3D volume at the given voxel coordinates using nearest neighbor interpolation.
}
\note{
A coordinate which is exactly between two voxels is resolved towards the larger voxel index, i.e., \code{floor(coord + 0.5)} is used. This matches the behavior of \code{scipy.ndimage.map_coordinates(..., order = 0)}. Note that R's \code{round} uses banker's rounding and would give different results for such coordinates.
}
\keyword{internal}
